<Window x:Class="Insolation.MVVMs.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Insolation.Config"
        mc:Ignorable="d"
        Title="ConfigurationView" Height="450" Width="800">
    <Window.Resources>
        <ObjectDataProvider x:Key="EnumDataSource"
                        ObjectType="{x:Type sys:Enum}"
                        MethodName="GetValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:SunCoorCalcMethod"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Широта:"/>
            <TextBox Text="{Binding Latitude}" />

            <Label Content="Долгота:"/>
            <TextBox Text="{Binding Longitude}" />

            <Label Content="Часовой пояс (UTC):"/>
            <ComboBox ItemsSource="{Binding TimeZones}" SelectedItem="{Binding TimeZone}" />

            <Label Content="Метод Расчёта:"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource EnumDataSource}}"  SelectedItem="{Binding CalcMethod}"/>

            <Label Content="Отступ от рассвета(заката) по ГОСТ (часы):"/>
            <TextBox Text="{Binding GOSToffset}" />

            <Label Content="День Расчёта:"/>
            <DatePicker SelectedDate="{Binding CalcDay}" />

            <Label Content="Точность (минуты):"/>
            <ComboBox ItemsSource="{Binding CompFrequencies}" SelectedItem="{Binding CompFreq}" />
        </StackPanel>
        
    </Grid>
</Window>
